{
  "name": "sieve-portal",
  "version": "1.0.0",
  "description": "Sieve — email suppression & validation portal. Upload bounced/unsubscribed lists, check addresses before you send, browse and manage suppression lists.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev:server": "node --watch src/server.js",
    "dev:client": "npm run dev --prefix frontend",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "seed:reset": "node -e \"require('fs').rmSync('data/sieve.db',{force:true}); require('fs').rmSync('data/sieve.db-wal',{force:true}); require('fs').rmSync('data/sieve.db-shm',{force:true}); require('./src/services/seed').seedIfEmpty();\""
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
